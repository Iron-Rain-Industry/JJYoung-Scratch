# Starting the Venv and python applications

# cd into the venv folder within GIT BASH

Run  `source venv/Scripts/activate`

Run `python manage.py runserver`

##Creating the app in a new virutal (ubuntu)
    #sudo apt-get install python3-pip

    #sudo pip3 install virtualenv
    #virtualenv venv
    #source venv/bin/activate
    ###installs, creates then runs a venv

    #pip install -r requirements.txt
    ###imports the previous venv i.e fitness-app
